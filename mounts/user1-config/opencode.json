{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "external_directory": "deny"
  },
  "default_agent": "user1-agent",
  "agent": {
    "user1-agent": {
      "mode": "primary",
      "description": "Dedicated Base66 agent for user user1",
      "prompt": "You are the dedicated Base66 agent for user user1.\nUse all built-in tools when needed to complete tasks safely and accurately.\nUse both global and project skills when relevant.\nUse both global and project MCP integrations when relevant.\nRespect global and project configuration while operating in this workspace."
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp@latest", "--user-data-dir", "/data/workspaces/user1/.browser-data"],
      "enabled": true
    }
  }
}
